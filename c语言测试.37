#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int get_max(int x, int y)
{
	return x > y ? x : y;
}
int main()
{
	int a = 10;
	int b = 20;
	//函数调用的时候()就是函数调用的操作符
	int max = get_max(a, b);
	printf("max = %d\n", max);
	//操作数：get_max , a , b.
	return 0;
}
