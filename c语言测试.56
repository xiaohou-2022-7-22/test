#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
void test(int arr[])
{
	int sz = sizeof(arr) / sizeof(arr[0]);
	printf("%d\n", sz);//32-bit--1,64-bit--2.
}
int main()
{
	int arr[10] = { 0 };
	test(arr);
	return 0;
}
