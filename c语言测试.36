#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int a = 0;
	int b = 0;
	if (a > 5)
		b = 3;
	else
		b = -3;
	printf("%d\n", b);
	return 0;
}
//以上代码可以将
if (a > 5)
		b = 3;
	else
		b = -3;
    //改写成
    b = (a > 5? 3:-3);
